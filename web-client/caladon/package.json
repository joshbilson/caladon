{
  "name": "@caladon/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Caladon web client protocol SDK: seed/passkey identity -> caladon-core WASM -> live gateway protocol (onboard, attestation verify, sealed session, sealed chat). The committable, testable INJECT layer (SURGERY.md §D). Browser + Node isomorphic.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build:wasm": "./build-wasm.sh",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:live": "CALADON_LIVE=1 vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
