{
  "name": "@caladon/web-shim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stateless Hono proxy shim for the Caladon web client. NO DB, NO keys. Routes /api/caladon/{chat,messages,attestation,session} + /pcs-collateral -> gw.caladon.ai. HttpOnly session cookie; forwards opaque envelopes only.",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx src/smoke.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
